version: '3'
services:
  postgres:
    name: tuotearvostelu
    image: postgres
    environment:
      - POSTGRES_DB=arvostelu
      - POSTGRES_USER=arvostelu
      - POSTGRES_PASSWORD=arvostelu
    ports:
      - "5432:5432"
